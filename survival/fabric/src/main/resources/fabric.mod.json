{
    "schemaVersion": 1,
    "id": "${mod_id}",
    "version": "${version}",
    "name": "${mod_name}",
    "description": "${description}",
    "authors": [
        "${mod_author}"
    ],
    "license": "${license}",
    "environment": "*",
    "entrypoints": {
        "main": [
            {
                "adapter": "kotlin",
                "value": "dev.zoenetic.brokenpromises.survival.fabric.SurvivalFabric"
            }
        ],
        "client": [
            {
                "adapter": "kotlin",
                "value": "dev.zoenetic.brokenpromises.survival.fabric.SurvivalClientFabric"
            }
        ]
    },
    "mixins": [
        "${mod_id}.mixins.json",
        "${mod_id}.fabric.mixins.json"
    ],
    "depends": {
        "fabricloader": ">=${fabric_loader_version}",
        "fabric-api": "*",
        "fabric-language-kotlin": ">=1.13.13",
        "minecraft": "~${minecraft_version}",
        "java": ">=${java_version}"
    }
}
